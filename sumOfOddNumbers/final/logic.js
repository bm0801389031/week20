//function named rowSumOddNumbers with parameter n
function rowSumOddNumbers(n){
    //return Math.pow with n as the base, and second parameter
    //to the exponent or power
    // 1. To find the sum of a certain row in a triangle
    // of odd numbers all that you have to do is number of row to power of three
        return Math.pow(n, 3);
    }